<Window x:Class="CouseWork3Semester.Views.SettlementsView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Settlements Manager" Height="640" Width="900"
        WindowStartupLocation="CenterScreen">
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Форма создания заселения -->
        <GroupBox Header="Create Settlement" Margin="0,0,0,10">
            <StackPanel Margin="10">
                <StackPanel Orientation="Horizontal" Margin="0,0,0,10">
                    <StackPanel Width="300" Margin="0,0,10,0">
                        <TextBlock Text="Select room"/>
                        <ComboBox x:Name="RoomSelectCombo"
                                  DisplayMemberPath="Display"
                                  SelectedValuePath="Room"
                                  Height="28"/>
                    </StackPanel>
                    <StackPanel Width="260" Margin="0,0,10,0">
                        <TextBlock Text="Document"/>
                        <ComboBox x:Name="DocumentSelectCombo"
                                  DisplayMemberPath="Title"
                                  Height="28"/>
                    </StackPanel>
                    <StackPanel Width="180" Margin="0,0,10,0">
                        <TextBlock Text="Settlement Date"/>
                        <DatePicker x:Name="SettlementDatePicker"/>
                    </StackPanel>
                </StackPanel>

                <TextBlock Text="Select occupants to settle (from registry)"
                           FontWeight="SemiBold" Margin="0,0,0,5"/>
                <ListBox x:Name="OccupantsSelectList"
                         SelectionMode="Extended"
                         Height="180">
                    <ListBox.ItemTemplate>
                        <DataTemplate>
                            <StackPanel Orientation="Horizontal" Margin="4,2">
                                <TextBlock Text="{Binding FullName}" Width="240"/>
                                <TextBlock Text="{Binding Type}" Width="120"/>
                                <TextBlock Text="{Binding BirthDate}" Width="140"/>
                            </StackPanel>
                        </DataTemplate>
                    </ListBox.ItemTemplate>
                </ListBox>

                <StackPanel Orientation="Horizontal" Margin="0,10,0,0">
                    <Button x:Name="CreateSettlementButton" Content="Add Settlement" Width="180" Margin="0,0,10,0"/>
                </StackPanel>
            </StackPanel>
        </GroupBox>

        <!-- Список заселений -->
        <GroupBox Grid.Row="1" Header="Settlements">
            <Grid>
                <DataGrid x:Name="SettlementsGrid"
                          AutoGenerateColumns="False"
                          IsReadOnly="True"
                          SelectionMode="Single"
                          Margin="5"
                          CanUserResizeRows="False"
                          EnableRowVirtualization="True"
                          EnableColumnVirtualization="True">
                    <DataGrid.Columns>
                        <DataGridTextColumn Header="ID" Binding="{Binding Id}" Width="220"/>
                        <DataGridTextColumn Header="Date" Binding="{Binding SettlementDate}" Width="120"/>
                        <DataGridTextColumn Header="Room" Binding="{Binding RoomNumber}" Width="80"/>
                        <DataGridTextColumn Header="Occupants" Binding="{Binding OccupantsCount}" Width="100"/>
                        <DataGridTextColumn Header="Document" Binding="{Binding DocumentTitle}" Width="200"/>
                    </DataGrid.Columns>
                </DataGrid>
            </Grid>
        </GroupBox>

        <!-- Строка состояния -->
        <StatusBar Grid.Row="2" Margin="0,10,0,0">
            <StatusBarItem>
                <TextBlock x:Name="StatusText" Text="Ready"/>
            </StatusBarItem>
        </StatusBar>
    </Grid>
</Window>