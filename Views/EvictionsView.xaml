<Window x:Class="CouseWork3Semester.Views.EvictionsView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Evictions Manager" Height="640" Width="900"
        WindowStartupLocation="CenterScreen">
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Форма создания выселения -->
        <GroupBox Header="Create Eviction" Margin="0,0,0,10">
            <StackPanel Margin="10">
                <StackPanel Orientation="Horizontal" Margin="0,0,0,10">
                    <StackPanel Width="300" Margin="0,0,10,0">
                        <TextBlock Text="Select room"/>
                        <ComboBox x:Name="RoomSelectCombo"
                                  DisplayMemberPath="Display"
                                  SelectedValuePath="Room"
                                  Height="28"/>
                    </StackPanel>
                    <StackPanel Width="280" Margin="0,0,10,0">
                        <TextBlock Text="Reason"/>
                        <TextBox x:Name="ReasonTextBox" Height="28"/>
                    </StackPanel>
                    <StackPanel Width="180" Margin="0,0,10,0">
                        <TextBlock Text="Eviction Date"/>
                        <DatePicker x:Name="EvictionDatePicker"/>
                    </StackPanel>
                </StackPanel>

                <TextBlock Text="Select occupants to evict (from current room occupants)"
                           FontWeight="SemiBold" Margin="0,0,0,5"/>
                <ListBox x:Name="OccupantsSelectList"
                         SelectionMode="Extended"
                         Height="180">
                    <ListBox.ItemTemplate>
                        <DataTemplate>
                            <StackPanel Orientation="Horizontal" Margin="4,2">
                                <TextBlock Text="{Binding FullName}" Width="260"/>
                                <TextBlock Text="{Binding Type}" Width="120"/>
                                <TextBlock Text="{Binding BirthDate}" Width="140"/>
                            </StackPanel>
                        </DataTemplate>
                    </ListBox.ItemTemplate>
                </ListBox>

                <StackPanel Orientation="Horizontal" Margin="0,10,0,0">
                    <Button x:Name="CreateEvictionButton" Content="Add Eviction" Width="180" Margin="0,0,10,0"/>
                </StackPanel>
            </StackPanel>
        </GroupBox>

        <!-- Список выселений -->
        <GroupBox Grid.Row="1" Header="Evictions">
            <Grid>
                <DataGrid x:Name="EvictionsGrid"
                          AutoGenerateColumns="False"
                          IsReadOnly="True"
                          SelectionMode="Single"
                          Margin="5"
                          CanUserResizeRows="False"
                          EnableRowVirtualization="True"
                          EnableColumnVirtualization="True">
                    <DataGrid.Columns>
                        <DataGridTextColumn Header="ID" Binding="{Binding Id}" Width="220"/>
                        <DataGridTextColumn Header="Date" Binding="{Binding EvictionDate}" Width="120"/>
                        <DataGridTextColumn Header="Room" Binding="{Binding RoomNumber}" Width="80"/>
                        <DataGridTextColumn Header="Occupants" Binding="{Binding OccupantsCount}" Width="100"/>
                        <DataGridTextColumn Header="Reason" Binding="{Binding Reason}" Width="260"/>
                    </DataGrid.Columns>
                </DataGrid>
            </Grid>
        </GroupBox>

        <!-- Строка состояния -->
        <StatusBar Grid.Row="2" Margin="0,10,0,0">
            <StatusBarItem>
                <TextBlock x:Name="StatusText" Text="Ready"/>
            </StatusBarItem>
        </StatusBar>
    </Grid>
</Window>